{{/* config */}}
{{- $computer_name := promptString "Device Name" "" -}}
{{- $install_asdf := promptBool "Install ASDF" false -}}
{{- $install_antigen := promptBool "Install Anitgen ZSH Plugin" false -}}

[data]
  computer_name = "{{ $computer_name }}"
  install_asdf = "{{ $install_asdf }}"
  install_antigen = "{{ $install_antigen }}"
[git]
  autoCommit = true
  autoPush = true
[edit]
  command = "code"
  args = "-w"